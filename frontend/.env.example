# Base URL for backend API (include /api because backend context-path is /api)
VITE_API_BASE_URL=https://your-render-service.onrender.com/api

# Optional: override for local development (Vite prioritizes .env.local)
# VITE_API_BASE_URL=http://localhost:8080/api
